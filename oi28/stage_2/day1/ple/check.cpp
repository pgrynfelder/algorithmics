#include <bits/stdc++.h>
using namespace std;

constexpr int N = 5e5 + 7;

long long W[N], C[N];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int n;
    int k = 0;
    cin >> n; 

    for (int i = 1; i <= n; i++) {
        cin >> W[N];
    }
    for (int i = 1; i <= n; i++) {
        cin >> C[i];
    }
    for (int i = 1; i <= n; i++) {
        long long c = C[i];
        
    }
    cout << k;

    return 0;
}