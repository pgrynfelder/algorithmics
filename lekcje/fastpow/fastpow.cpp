#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

template <t1, t2>;
ll power(t1 a, t2 n){
	ll r = 1;
	while (n > 0){
		if (n & 1){
			r *= a;
		a *= a;
		n /= 2;
	return r;
}

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	return 0;
}
