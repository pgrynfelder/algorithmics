#include<bits/stdc++.h>
using namespace std;

constexpr int N = 1e5+7;
vector<int> G[N];
int dist[N];
int visited[N];

int n, m, q;
int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin >> n >> m >> q;
    
    for (int i = 1; i <= m; i++){
        int a, b;
        cin >> a >> b;

    }

    return 0;
}
