#include<bits/stdc++.h>

using namespace std;

/*
 * mamy graf
 * i te krawedzie sa jakos wazone
 * i mamy znalezc sciezke
 * gdzie jakies jest minimalna albo maksymalna waga, nie suma wag
 *
 * to zrobmy dla mininalnej
 *
 * szukamy sb grafu, ze mozemy sie dostac wszedzie, czyli wsm drzewa, a max(wagi) jest minimalne
 *
 * no to czyscimy se krawedzie z grafu, czyli wsm nawet go nie tworzymy
 *
 * wpisujemy po kolei po wadze rosnacej
 * nie wpisujemy tych co sa juz polaczone
 * no i robimy sobie findunion
 */

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	return 0;
}
