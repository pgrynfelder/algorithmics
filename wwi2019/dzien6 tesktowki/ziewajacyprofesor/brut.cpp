#include<bits/stdc++.h>
using namespace std;


string word, word2;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, m;
    cin >> n >> m;
    cin >> word >> word2;

    for (int )
    return 0;
}
