#include<bits/stdc++.h>

using namespace std;

/* toposort dfs post order to a stack
 * create a transponed graph (with changed directions) 
 * dfs post order from vertices on the stack ommiting ones previously used in this step
 */

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	return 0;
}
